import { Controls, Meta, Primary, Stories, Title } from "@storybook/blocks";
import meta from './Paragraph.stories';

<Meta of={meta} />

# Paragraph

The `Paragraph` component is part of the Oakd design system, providing a standardized way to display text content. It ensures consistent styling and formatting across applications.

<Primary />

<Controls />

## Usage

The `Paragraph` component can be used to display any block of text. It supports custom class names and styles for additional customization.

<Stories />
