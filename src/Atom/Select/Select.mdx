import { Controls, Meta, Primary, Stories } from "@storybook/blocks";
import meta from './Select.stories';

<Meta of={meta} />

# Select

The `Select` component is a flexible dropdown selector that allows users to choose from a list of options. It supports categorization and provides accessibility enhancements like keyboard navigation and ARIA attributes.

<Primary />

<Controls />

# Select Component

The Select component allows users to select an option from a dropdown list. It accepts an array of options, supports optional categorization, and can render custom placeholder text via the `foo` prop.

<Stories />
