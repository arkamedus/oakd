import { Controls, Meta, Primary, Stories } from "@storybook/blocks";
import meta from './MultiLineChart.stories';

<Meta of={meta} />

# MultiLineChart

The `MultiLineChart` component renders multiple time-series lines with shared
colors, a consistent legend, and a hover tooltip for comparing values at the
same index.

<Primary />
<Controls />
<Stories />
