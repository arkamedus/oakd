import { Controls, Meta, Primary, Stories } from "@storybook/blocks";
import meta from './EmbeddingHeatmap.stories';

<Meta of={meta} />

# EmbeddingHeatmap

The `EmbeddingHeatmap` component visualizes either a one-dimensional embedding
strip or a two-dimensional matrix using the shared viridis color scale.

<Primary />
<Controls />
<Stories />
