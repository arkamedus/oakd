import { Controls, Meta, Primary, Stories } from "@storybook/blocks";
import meta from './Input.stories';

<Meta of={meta} />

# Input

The `Input` component is a flexible text entry field that adheres to oakd Interactive’s design system. It supports multiple types, sizes, error states, disabled states, and optional icons. This component is designed for accessibility and ease-of-use, featuring proper aria attributes and clean, maintainable code.

<Primary />

<Controls />

# Input Component

Below are different variations of the Input component. Use this component to capture user input in forms and interactive interfaces. It integrates well with other oakd components and respects theming and responsive design.

<Stories />
