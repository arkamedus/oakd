import { Controls, Meta, Primary, Stories } from "@storybook/blocks";
import meta from "./Input.stories";

<Meta of={meta} />

# Input

The `Input` component is a flexible text entry field that adheres to oakd Interactive’s design system. It supports native input props alongside oakd-specific `variant`, `size`, `error`, and `icon` props.

<Primary />

<Controls />

# Input Component

Use the stories below to compare field states such as search inputs, validation errors, disabled fields, and ref-driven focus behavior.

<Stories />
