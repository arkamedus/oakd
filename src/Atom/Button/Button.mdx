import { Controls, Meta, Primary, Stories } from "@storybook/blocks";
import meta from './Button.stories';

<Meta of={meta} />

# Button

The `Button` component is an interactive element designed for user engagement. It supports multiple variants and sizes, accepts native button props, and can include icons and labels as needed.

<Primary />

<Controls />

---

# Button Component

The `Button` component provides a versatile control for actions such as submitting forms, navigation, and triggering events. It integrates with other oakd components like Space, Icon, and Paragraph to ensure design consistency and responsiveness.

**Props**:

- `variant`: Sets the visual style of the button (e.g., default, primary, danger, warning, ghost, disabled).
- `htmlType`: Sets the native HTML button type (`button`, `submit`, or `reset`).
- `size`: Adjusts the size of the button (default, small, large).
- `disabled`: Disables the button and prevents interactions.
- `icon`: Optionally displays an icon either as a string (icon name) or as a JSX element.
- Native button props such as `aria-*`, `data-*`, and keyboard handlers pass through to the rendered `<button>`.

<Stories />
