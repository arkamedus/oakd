import { Controls, Meta, Primary, Stories } from "@storybook/blocks";
import meta from './StackedBreakdownChart.stories';

<Meta of={meta} />

# StackedBreakdownChart

The `StackedBreakdownChart` component renders stacked percentage bars with the
shared oakd chart legend colors.

<Primary />
<Controls />
<Stories />
