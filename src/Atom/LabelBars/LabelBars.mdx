import { Controls, Meta, Primary, Stories } from "@storybook/blocks";
import meta from './LabelBars.stories';

<Meta of={meta} />

# LabelBars

The `LabelBars` component shows ranked label probabilities with the shared oakd
chart color scale.

<Primary />
<Controls />
<Stories />
