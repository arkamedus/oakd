import { Controls, Meta, Primary, Stories } from "@storybook/blocks";
import meta from "./Breadcrumb.stories";

<Meta of={meta} />

# Breadcrumb

The `Breadcrumb` component displays a navigational hierarchy that helps users understand their location within a website or application. It renders semantic navigation markup with a list of breadcrumb items, and marks the current page with `aria-current="page"`.

<Primary />

<Controls />

# Breadcrumb Component

The stories below show common breadcrumb patterns such as route paths, mixed content, and icon-led roots.

<Stories />
