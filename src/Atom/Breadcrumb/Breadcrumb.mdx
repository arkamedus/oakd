import { Controls, Meta, Primary, Stories } from "@storybook/blocks";
import meta from './Breadcrumb.stories';

<Meta of={meta} />

# Breadcrumb

The `Breadcrumb` component displays a navigational hierarchy that helps users understand their location within a website or application. It shows the path from the homepage to the current page, with each segment of the path being a clickable link (except for the last item, which is typically a static label).

<Primary />

<Controls />

# Breadcrumb Component

Below are examples of the `Breadcrumb` component with different configurations:

<Stories />
